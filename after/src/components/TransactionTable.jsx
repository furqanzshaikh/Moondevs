const TransactionTable = ({ burnTransactions, coinData }) => {
    return (
        <div className="header">
            <p className="header_label">Burn Transactions</p>
            {/* Remaining JSX for displaying the transaction table */}
        </div>
    );
};

export default TransactionTable;